# Geradores de dados sintéticos
Faker==25.0.0

# Ingestão (próxima etapa)
psycopg2-binary==2.9.9
mysql-connector-python==8.3.0
pymongo==4.6.3
pandas==2.2.2
pyarrow==15.0.2
requests==2.31.0
minio==7.2.7

# Validações
validate-docbr==1.10.0
unidecode==1.3.8

# Transformação
dbt-core==1.11.11
dbt-duckdb==1.10.1
duckdb>=1.0.0

# Observabilidade de dados (CLI `edr` para gerar dashboard HTML;
# o package dbt vem por `dbt deps`, não por pip).
elementary-data>=0.16.0

# API e mock
fastapi==0.110.0
uvicorn==0.29.0
