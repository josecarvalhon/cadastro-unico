# Dependências para o pipeline rodar dentro do container Airflow.
# Versões fixadas para reprodutibilidade.

# Ingestão Bronze
psycopg2-binary==2.9.9
mysql-connector-python==8.3.0
pymongo==4.6.3
pandas==2.2.2
pyarrow==15.0.2
requests==2.31.0
minio==7.2.7

# Transformação dbt
dbt-core==1.11.11
dbt-duckdb==1.10.1
duckdb>=1.0.0

# Observabilidade (CLI `edr`)
elementary-data>=0.16.0
